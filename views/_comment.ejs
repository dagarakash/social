<li id="comment-<%= comment._id %>">
        <p>
            
           <div style=" font-size:1.2rem; font-weight: 100;">  <%= comment.content %></div>
            <br>
           
            <%= comment.user.name %>
              
           
            <!-- CHANGE :: display the likes of this comment, if the user is logged in, then show the link to toggle likes, else, just show the count -->
            <!-- study about the data attribute -->
            <small>
                <% if (locals.user){ %>
                    <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
                            <%= comment.likes.length %> Likes
                    </a>
                <% }else{ %>
                    <%= comment.likes.length %> Likes
                <% } %>
            </small>
            <% if (locals.user && locals.user.id == comment.user._id){ %>
            
                <small>
                    <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>"> <button style="margin-left: 6px;">Delete</button> </a>
                </small>
                <% } %>
        </p>    
       
</li>